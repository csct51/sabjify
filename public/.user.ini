; Raised upload limits for the Laravel app.
; These override the PHP server defaults (upload_max_filesize / post_max_size)
; so large product/category/recipe/logo images can be uploaded.
; Both directives are PHP_INI_PERDIR and can be set here per-project.
upload_max_filesize = 20M
post_max_size = 25M
